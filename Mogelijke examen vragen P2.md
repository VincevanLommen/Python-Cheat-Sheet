# Mogelijke examenvragen P2

## Abstracte classes
- Gebruik van `super()` zonder argumenten  
- Geen instantie maken van een abstracte klasse; alleen child classes kunnen worden geïnstantieerd  
- Werken met overschrijven (override) en `super`  

## Lijsten & lambda’s
- `new_list = list.sort()` retourneert `None` → gebruik `new_list = sorted(list)`  
- Geneste for-loops in de correcte volgorde: altijd van buiten naar binnen  
- `zip` gebruiken met twee lijsten van ongelijke lengte  
- Unieke waarden met een comprehension → werken met een `set`  


## Recursie
- Niet wagen met een counter of met een lus bende verkeert

## Dataframe
- A–Z → ascending sorteren
- 10 bewerkingen op een dataframe uitvoeren en daarna 1 index opvragen
- `dropna()` past het originele dataframe niet aan!
- CSV inlezen en daarna `.head()` printen
- Schriftelijk examen: 1 juist antwoord